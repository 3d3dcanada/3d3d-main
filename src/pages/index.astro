---
import BaseLayout from '../layouts/BaseLayout.astro';
import CampaignStrip from '../components/CampaignStrip';
import HeroSection from '../components/HeroSection';

const orgSchema = {
  '@context': 'https://schema.org',
  '@type': ['ProfessionalService', 'Organization'],
  name: '3D3D',
  url: 'https://3d3d.ca',
  description: 'Specialty fabrication for marine, motorsport, replacement parts, and field deployment. Founder-operated and serving worldwide buyers.',
  telephone: '+1-506-953-2678',
  email: 'info@3d3d.ca',
  areaServed: ['Worldwide', 'Caribbean', 'Coastal United States', 'Europe'],
  founder: { '@type': 'Person', name: 'Ken' },
  sameAs: [
    'https://www.youtube.com/@3D3Dcanada',
    'https://www.tiktok.com/@3d3dcanada',
    'https://www.instagram.com/3d3dca/',
    'https://www.facebook.com/profile.php?id=61575548079847',
    'https://www.printables.com/@KTK3D_3050116',
    'https://github.com/3d3dcanada/3d3d-main',
    'https://wa.me/15069532678',
    'https://ko-fi.com/3d3dca',
  ],
  image: 'https://3d3d.ca/media/real/fleet-start-press.jpg',
  logo: 'https://3d3d.ca/media/brand/logo-badge.webp',
};

const homeNavGroups = [
  {
    label: 'On This Page',
    items: [
      { id: 'home', label: 'Home', href: '/' },
      { id: 'what-i-do', label: 'What I Do', href: '#what-i-do' },
      { id: 'workshop', label: 'The Workshop', href: '#workshop' },
      { id: 'contact', label: 'Contact', href: '#contact' },
    ],
  },
  {
    label: 'Site',
    items: [
      { id: 'blog', label: 'Blog', href: '/blog' },
      { id: 'shop', label: 'Shop', href: '/shop' },
      { id: 'prusa', label: 'Prusa', href: '/prusa' },
      { id: 'about', label: 'About', href: '/about' },
    ],
  },
];

const services = [
  {
    id: 'marine',
    drawerId: 'drawer-marine',
    eyebrow: 'Marine Hardware',
    headline: 'The part does not exist anymore. So I build it.',
    description: 'Supply chains do not care about your passage plan. When a stainless pawl wears through a bronze pocket on a discontinued Barient 27 winch, or a Spinlock XA clutch handle snaps under a loaded halyard, you cannot wait three weeks for a mismatched part that may not fit. I reverse-engineer deck fittings, fairleads, winch components, and custom brackets, all built to handle offshore dynamic loads.',
    drawer: {
      materials: 'ASA above deck. UV-stable, 93°C heat deflection. PETG-CF below deck for structural fittings. PP Glass Fiber or PP Carbon Fiber for constant saltwater contact. TPU 95A for seals and gaskets. PA11 CF near engines. 192°C, resists oils and fuels.',
      turnaround: 'Send the broken component, or provide precise caliper measurements. If you\'re offshore, dimensions by message are enough. I\'ve built from blurry photos and measurements taken on a rolling deck.',
    },
  },
  {
    id: 'automotive',
    drawerId: 'drawer-automotive',
    eyebrow: 'Automotive Restoration',
    headline: 'Unobtainium ceases to be a problem.',
    description: 'I spent ten years turning wrenches on heavy iron. I have personally restored the AMC AMX, Dodge Polara, GTX, and Roadrunner. I know the frustration of halting a multi-year build because an interior trim panel, a dash overlay, or a specific retaining clip was last stamped forty years ago. I fabricate exact geometric replicas of discontinued OEM parts that fit and hold.',
    drawer: {
      materials: 'PA11 CF for engine bay. 192°C heat deflection, resists oils and fuels. PETG-CF for structural brackets. ASA for exterior and underhood UV exposure. PC Blend for impact-critical parts. PEI 1010 when nothing else survives.',
      turnaround: 'Ship the shattered original piece or the surrounding housing. I will rebuild the geometry digitally and print the final part. For discontinued OEM parts, bring the original. I measure, model, and test-fit before committing.',
    },
  },
  {
    id: 'onsite',
    drawerId: 'drawer-onsite',
    eyebrow: 'On-Site Deployment',
    headline: 'The vessel cannot move. I can.',
    description: 'Marina fees bleed cruising budgets dry. If your vessel is tied up in Antigua, Newport, or a remote boatyard waiting on a critical refit component, the fastest path to the water is bringing the factory to the dock. I travel worldwide with the necessary equipment, assess the failure, design the fix, and fabricate the hardware on location.',
    drawer: {
      materials: 'Full material stack on location. ASA, PA11 CF, PETG-CF, TPU, PP-CF, matched to the failure mode after I\'ve assessed it in person. No guessing from a product page.',
      turnaround: 'Call me. Tell me where the boat is and what is broken. I will pack the cases and sort the flights.',
    },
  },
  {
    id: 'remote',
    drawerId: 'drawer-remote',
    eyebrow: 'Remote Fabrication',
    headline: 'Engineered here. Deployed anywhere.',
    description: 'For components that do not require my physical presence on deck. Send high-resolution photos, the load requirements, and exact caliper measurements. I design the replacement and ship the finished hardware to your current coordinates. Your marina, your boatyard, wherever the vessel is sitting.',
    drawer: {
      materials: 'Material matched to the environment after intake. Saltwater, UV, heat, chemical exposure, dynamic load. I specify based on what the part has to survive, not what is easiest to print.',
      turnaround: 'Send photos of the broken component, the mounting area, and caliper measurements. I will confirm the geometry, quote the job, and ship to your coordinates.',
    },
  },
];

---

<BaseLayout
  title="3D3D | You Have a Problem. I Am Your Solution."
  description="Offshore marine fabrication and discontinued part replacement. Worldwide on-site deployment and remote engineering by a 20,000nm offshore specialist."
  schema={orgSchema}
  activePath="/"
  navGroups={homeNavGroups}
  bodyClass="home-editorial"
>
  <div class="home-page">

    <!-- ═══════════════════════════════════════════════════
         HERO (React + Motion)
         ═══════════════════════════════════════════════════ -->
    <HeroSection client:load />

    <!-- ═══════════════════════════════════════════════════
         CAMPAIGN STRIP (React + Motion)
         ═══════════════════════════════════════════════════ -->
    <CampaignStrip client:visible />

    <!-- ═══════════════════════════════════════════════════
         WHAT WE DO — light
         ═══════════════════════════════════════════════════ -->
    <section id="what-i-do" class="home-section home-section--light work-section" data-section aria-labelledby="work-label">
      <div class="section-shell work-shell">

        <header class="work-header">
          <p class="eyebrow scroll-in-left">What I Do</p>
          <h2 class="section-title scroll-in" aria-label="I scan it. I model it. I build it.">
            <span class="typewriter-line" data-typewriter>I scan it.</span><br />
            <span class="typewriter-line" data-typewriter>I model it.</span><br />
            <span class="typewriter-line" data-typewriter>I build it.</span>
          </h2>
          <p class="section-body scroll-in-fade scroll-delay-1">
            My background is not in software. It is in keeping complex machinery alive when failure has severe consequences. I reverse-engineer broken and obsolete components into exact-fit replacements: deck fittings, winch hardware, discontinued OEM parts, vintage automotive pieces, and one-off solutions that no supply chain carries anymore. Bring me the part, a photo, or just the problem.
          </p>
        </header>

        <div class="services-scroll" role="region" aria-label="Service areas" tabindex="0">
          {services.map((service, i) => (
            <article
              class="service-card scroll-in"
              style={`transition-delay: ${i * 0.12}s;`}
              data-drawer-trigger
              data-drawer-id={service.drawerId}
              role="button"
              tabindex="0"
            >
              <p class="service-card__eyebrow">{service.eyebrow}</p>
              <h3 class="service-card__headline">{service.headline}</h3>
              <p class="service-card__desc">{service.description}</p>
              <span class="service-card__cta">Learn more →</span>
            </article>
          ))}
        </div>

      </div>
    </section>

    <!-- ═══════════════════════════════════════════════════
         THE WORKSHOP — dark
         ═══════════════════════════════════════════════════ -->
    <section id="workshop" class="home-section home-section--dark workshop-section" data-section aria-labelledby="workshop-label">
      <div class="section-shell workshop-shell">
        <header class="workshop-header">
          <p class="eyebrow eyebrow--light scroll-in-left">The Workshop</p>
          <h2 class="section-title section-title--light scroll-in" id="workshop-label">
            Built to solve problems.<br />Not to look pretty on a shelf.
          </h2>
          <p class="section-body section-body--inverse scroll-in-fade scroll-delay-1">
            Two Prusa MK4S printers running engineering compounds. A full mechanical tool kit built over twenty years of turning wrenches. Calipers, micrometers, and a scanner for reverse engineering parts that no longer have drawings. This is not a hobby setup. It is a portable fabrication lab that travels to the job.
          </p>
        </header>

        <div class="workshop-grid">
          <figure class="workshop-card workshop-card--hero scroll-in" data-lightbox data-lightbox-src="/media/workshop/printer-action-cf.jpg" data-lightbox-label="Live print" data-lightbox-note="PETG-CF structural bracket, 0.2mm layer height. Carbon fiber reinforced compound printed on the Prusa MK4S with a hardened steel nozzle. This bracket replaces a factory aluminium piece that cracked under cyclic loading." role="button" tabindex="0">
            <img src="/media/workshop/printer-action-cf.jpg" alt="Prusa MK4S printing carbon fiber compound" loading="lazy" decoding="async" />
            <figcaption>
              <span class="workshop-card__label">Live print</span>
              <span class="workshop-card__note">PETG-CF structural bracket, 0.2mm layer height</span>
            </figcaption>
          </figure>
          <figure class="workshop-card scroll-in scroll-delay-1" data-lightbox data-lightbox-src="/media/workshop/printer-frame-shot.jpg" data-lightbox-label="The machine" data-lightbox-note="Prusa MK4S with hardened steel nozzle for abrasive compounds like PETG-CF, PA11-CF, and PP Glass Fiber. Input shaping enabled for high-speed printing without ringing artifacts." role="button" tabindex="0">
            <img src="/media/workshop/printer-frame-shot.jpg" alt="Prusa printer frame close-up" loading="lazy" decoding="async" />
            <figcaption>
              <span class="workshop-card__label">The machine</span>
              <span class="workshop-card__note">Prusa MK4S with hardened nozzle for abrasive compounds</span>
            </figcaption>
          </figure>
          <figure class="workshop-card scroll-in scroll-delay-2" data-lightbox data-lightbox-src="/media/workshop/machine-parts-flatlay.jpg" data-lightbox-label="Output" data-lightbox-note="Finished parts ready for fitment and deployment. Each component is printed in the compound matched to its failure mode and environment. Nothing leaves the bench until dimensional checks pass." role="button" tabindex="0">
            <img src="/media/workshop/machine-parts-flatlay.jpg" alt="Finished machine parts laid out for inspection" loading="lazy" decoding="async" />
            <figcaption>
              <span class="workshop-card__label">Output</span>
              <span class="workshop-card__note">Finished parts ready for fitment and deployment</span>
            </figcaption>
          </figure>
          <figure class="workshop-card scroll-in scroll-delay-1" data-lightbox data-lightbox-src="/media/workshop/modular-parts-flatlay.jpg" data-lightbox-label="Modular builds" data-lightbox-note="Replacement components printed in matched compounds. Modular design allows field assembly and individual piece replacement without reprinting the entire assembly." role="button" tabindex="0">
            <img src="/media/workshop/modular-parts-flatlay.jpg" alt="Modular replacement parts array" loading="lazy" decoding="async" />
            <figcaption>
              <span class="workshop-card__label">Modular builds</span>
              <span class="workshop-card__note">Replacement components printed in matched compounds</span>
            </figcaption>
          </figure>
          <figure class="workshop-card scroll-in scroll-delay-2" data-lightbox data-lightbox-src="/media/workshop/rv-lab-01.jpg" data-lightbox-label="Mobile lab" data-lightbox-note="The whole operation travels. Worldwide deployment ready. Two printers, full tool kit, scanner, and materials library packed for on-site fabrication at marinas, boatyards, and race events." role="button" tabindex="0">
            <img src="/media/workshop/rv-lab-01.jpg" alt="Mobile fabrication lab interior" loading="lazy" decoding="async" />
            <figcaption>
              <span class="workshop-card__label">Mobile lab</span>
              <span class="workshop-card__note">The whole operation travels. Worldwide deployment ready.</span>
            </figcaption>
          </figure>
        </div>

        <div class="workshop-stats">
          <div class="workshop-stat scroll-in">
            <span class="workshop-stat__value">17+</span>
            <span class="workshop-stat__label">Compounds in the stack</span>
          </div>
          <div class="workshop-stat scroll-in scroll-delay-1">
            <span class="workshop-stat__value">207°C</span>
            <span class="workshop-stat__label">Max heat deflection (PEI 1010)</span>
          </div>
          <div class="workshop-stat scroll-in scroll-delay-2">
            <span class="workshop-stat__value">20,000+</span>
            <span class="workshop-stat__label">Nautical miles offshore</span>
          </div>
          <div class="workshop-stat scroll-in scroll-delay-3">
            <span class="workshop-stat__value">10 yr</span>
            <span class="workshop-stat__label">Professional mechanic background</span>
          </div>
        </div>
      </div>
    </section>

    <!-- ═══════════════════════════════════════════════════
         CONTACT — teal/dark
         ═══════════════════════════════════════════════════ -->
    <section id="contact" class="home-section home-section--teal final-cta-section" data-section>
      <div class="section-shell final-cta-shell">
        <div class="final-cta-copy">
          <h2 class="section-title section-title--inverse scroll-in">
            One call. I'll assess it.<br />We'll sort it out.
          </h2>
          <p class="section-body section-body--inverse scroll-in-fade scroll-delay-1">
            Tell me the problem. Tell me where the vessel is. I will tell you whether I can solve it, how we will do it, and exactly what it takes. That is the whole process.
          </p>
          <div class="final-cta-actions">
            <a href="/contact" class="final-cta-button final-cta-button--light scroll-in scroll-delay-2">Contact Ken →</a>
            <a href="/book" class="final-cta-button final-cta-button--ghost scroll-in scroll-delay-3">Book a Call →</a>
          </div>
        </div>
      </div>
    </section>

    <!-- ═══════════════════════════════════════════════════
         OFF-CANVAS DRAWER
         ═══════════════════════════════════════════════════ -->
    <div class="drawer" id="service-drawer" data-open="false" hidden>
      <div class="drawer__backdrop" data-drawer-close></div>
      <aside class="drawer__panel" role="dialog" aria-modal="true" aria-label="Service details">
        <button class="drawer__close" data-drawer-close aria-label="Close">✕</button>
        <div class="drawer__content" id="drawer-content"></div>
      </aside>
    </div>

    {services.map((service) => (
      <template id={service.drawerId}>
        <div class="drawer-copy">
          <p class="drawer-copy__eyebrow">{service.eyebrow}</p>
          <h3 class="drawer-copy__title">{service.headline}</h3>
          <p class="drawer-copy__stakes">{service.description}</p>
          <div class="drawer-copy__notes">
            <div>
              <span>Materials & compounds</span>
              <p>{service.drawer.materials}</p>
            </div>
            <div>
              <span>How to start</span>
              <p>{service.drawer.turnaround}</p>
            </div>
          </div>
          <a href="/contact" class="drawer-copy__cta">Request this type of work</a>
        </div>
      </template>
    ))}
  </div>

  <!-- ═══════════════════════════════════════════════════
       IMAGE LIGHTBOX
       ═══════════════════════════════════════════════════ -->
  <div class="lightbox" id="image-lightbox" data-open="false" hidden>
    <div class="lightbox__backdrop" data-lightbox-close></div>
    <div class="lightbox__container">
      <button class="lightbox__close" data-lightbox-close aria-label="Close">&times;</button>
      <img class="lightbox__img" id="lightbox-img" src="" alt="" />
      <div class="lightbox__info">
        <span class="lightbox__label" id="lightbox-label"></span>
        <p class="lightbox__note" id="lightbox-note"></p>
      </div>
    </div>
  </div>

  <!-- ═══════════════════════════════════════════════════
       SCRIPTS
       ═══════════════════════════════════════════════════ -->
  <script is:inline>
    (() => {
      const drawer = document.getElementById('service-drawer');
      const content = document.getElementById('drawer-content');
      const triggers = Array.from(document.querySelectorAll('[data-drawer-trigger]'));

      if (!(drawer instanceof HTMLElement) || !(content instanceof HTMLElement) || triggers.length === 0) return;

      let closeTimer = 0;

      const closeDrawer = () => {
        drawer.dataset.open = 'false';
        document.body.classList.remove('drawer-open');
        window.clearTimeout(closeTimer);
        closeTimer = window.setTimeout(() => {
          drawer.hidden = true;
          content.innerHTML = '';
        }, 320);
      };

      const openDrawer = (templateId) => {
        const template = document.getElementById(templateId);
        if (!(template instanceof HTMLTemplateElement)) return;
        content.innerHTML = '';
        content.appendChild(template.content.cloneNode(true));
        drawer.hidden = false;
        window.requestAnimationFrame(() => {
          drawer.dataset.open = 'true';
          document.body.classList.add('drawer-open');
        });
      };

      triggers.forEach((trigger) => {
        const handler = () => {
          const id = trigger instanceof HTMLElement ? trigger.dataset.drawerId : null;
          if (id) openDrawer(id);
        };
        trigger.addEventListener('click', handler);
        trigger.addEventListener('keydown', (e) => {
          if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); handler(); }
        });
      });

      drawer.addEventListener('click', (event) => {
        const target = event.target instanceof Element ? event.target.closest('[data-drawer-close]') : null;
        if (!target) return;
        closeDrawer();
      });

      window.addEventListener('keydown', (event) => {
        if (event.key === 'Escape' && drawer.dataset.open === 'true') closeDrawer();
      });
    })();

    // Image lightbox for workshop cards
    (() => {
      const lightbox = document.getElementById('image-lightbox');
      const lbImg = document.getElementById('lightbox-img');
      const lbLabel = document.getElementById('lightbox-label');
      const lbNote = document.getElementById('lightbox-note');
      const triggers = Array.from(document.querySelectorAll('[data-lightbox]'));

      if (!lightbox || !lbImg || !lbLabel || !lbNote || triggers.length === 0) return;

      const closeLightbox = () => {
        lightbox.dataset.open = 'false';
        document.body.classList.remove('drawer-open');
        setTimeout(() => { lightbox.hidden = true; }, 280);
      };

      const openLightbox = (el) => {
        lbImg.src = el.dataset.lightboxSrc || '';
        lbImg.alt = el.dataset.lightboxLabel || '';
        lbLabel.textContent = el.dataset.lightboxLabel || '';
        lbNote.textContent = el.dataset.lightboxNote || '';
        lightbox.hidden = false;
        requestAnimationFrame(() => {
          lightbox.dataset.open = 'true';
          document.body.classList.add('drawer-open');
        });
      };

      triggers.forEach((t) => {
        t.addEventListener('click', () => openLightbox(t));
        t.addEventListener('keydown', (e) => {
          if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); openLightbox(t); }
        });
      });

      lightbox.addEventListener('click', (e) => {
        if (e.target.closest('[data-lightbox-close]')) closeLightbox();
      });

      window.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && lightbox.dataset.open === 'true') closeLightbox();
      });
    })();

    // Campaign strip and modals handled by CampaignStrip React component

    // Typewriter effect for "I scan it. I model it. I build it."
    (() => {
      const lines = Array.from(document.querySelectorAll('[data-typewriter]'));
      if (lines.length === 0) return;
      let started = false;

      const runTypewriter = () => {
        if (started) return;
        started = true;
        lines.forEach((line, i) => {
          setTimeout(() => {
            line.classList.add('typing');
            line.addEventListener('animationend', () => {
              line.classList.remove('typing');
              line.classList.add('typed');
            }, { once: true });
          }, i * 700);
        });
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            runTypewriter();
            observer.disconnect();
          }
        });
      }, { threshold: 0.3 });

      observer.observe(lines[0].closest('.work-header') || lines[0]);
    })();
  </script>
</BaseLayout>

<style>
  /* ═══════════════════════════════════════════════════
     PAGE TOKENS
     ═══════════════════════════════════════════════════ */
  :global(body.home-editorial) {
    background: #F6F7FA;
    background-image: none;
  }

  :global(body.drawer-open) {
    overflow: hidden;
  }

  .home-page {
    --page-accent: #40C4C4;
    --page-accent-secondary: #E84A8A;
    --page-bg-light: #F6F7FA;
    --page-bg-dark: #111214;
    --page-surface: #FFFFFF;
    --page-text: #1A1A1A;
    --page-text-muted: #666666;
    --page-border: 2px solid #1A1A1A;
    --page-thick-border: 3px solid #1A1A1A;
    --page-shell-width: min(100% - clamp(1.5rem, 4vw, 4rem), 1320px);
    background: var(--page-bg-light);
    color: var(--page-text);
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .section-shell {
    position: relative;
    z-index: 1;
    width: var(--page-shell-width);
    margin: 0 auto;
  }

  .home-section {
    position: relative;
    overflow: clip;
    padding-block: clamp(4.5rem, 8vw, 8rem);
    scroll-margin-top: 1rem;
  }

  .home-section--light { background: var(--page-bg-light); }

  .home-section--dark {
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.02), rgba(255, 255, 255, 0)),
      var(--page-bg-dark);
    color: #F6F7FA;
  }

  .home-section--teal {
    background: var(--page-accent);
    color: #FFFFFF;
  }

  /* Scroll animations handled by global scroll-reveal.css */

  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.8rem;
    margin: 0;
    font-family: var(--font-tech);
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .eyebrow::before {
    content: '';
    width: 2.75rem;
    height: 2px;
    background: var(--page-accent);
    flex-shrink: 0;
  }

  .section-title {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 1.5rem + 4vw, 4.9rem);
    line-height: 0.98;
    letter-spacing: -0.035em;
    text-wrap: balance;
  }

  .section-title--light,
  .section-title--inverse { color: #F6F7FA; }

  .section-body {
    margin: 0;
    max-width: 42rem;
    font-size: clamp(1rem, 0.94rem + 0.35vw, 1.18rem);
    line-height: 1.75;
    color: var(--page-text);
  }

  .section-body--inverse { color: rgba(255, 255, 255, 0.9); }

  /* ═══════════════════════════════════════════════════
     HERO
     ═══════════════════════════════════════════════════ */
  .home-hero {
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding-top: clamp(5rem, 8vw, 7rem);
  }

  /* Background + overlays */
  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    will-change: transform;
  }

  .hero-bg-img {
    width: 100%;
    height: 115%;  /* extra height for Ken Burns + parallax */
    object-fit: cover;
    opacity: 0.32;
    filter: brightness(0.55);
    animation: kenburns 22s ease-in-out infinite alternate;
    transform-origin: center center;
  }

  @keyframes kenburns {
    0%   { transform: scale(1)    translate(0,    0); }
    100% { transform: scale(1.10) translate(-1.5%, 0.8%); }
  }

  /* SVG noise texture */
  .hero-grain {
    position: absolute;
    inset: 0;
    z-index: 1;
    width: 100%;
    height: 100%;
    pointer-events: none;
    mix-blend-mode: overlay;
  }

  /* Blueprint-style teal grid */
  .hero-grid {
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;
    background-image:
      linear-gradient(rgba(64,196,196,0.028) 1px, transparent 1px),
      linear-gradient(90deg, rgba(64,196,196,0.028) 1px, transparent 1px);
    background-size: 64px 64px;
  }

  /* Edge vignette */
  .hero-vignette {
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;
    background: radial-gradient(ellipse 110% 80% at 50% 50%, transparent 40%, rgba(8,10,11,0.65) 100%);
  }

  /* Layout */
  .hero-shell {
    display: grid;
    grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
    align-items: center;
    gap: clamp(2rem, 4vw, 4rem);
  }

  .hero-copy { display: grid; gap: 1.5rem; }

  .hero-eyebrow {
    color: rgba(246, 247, 250, 0.72);
    margin: 0;
  }

  .hero-eyebrow-inner {
    font-family: 'Courier New', monospace;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  /* Title */
  .hero-title {
    margin: 0;
    display: grid;
    gap: 0.08em;
    font-family: var(--font-display);
    font-size: clamp(3rem, 1.8rem + 5vw, 6.6rem);
    line-height: 0.92;
    letter-spacing: -0.04em;
    color: #FFFFFF;
    text-transform: uppercase;
  }

  /* Each line is a clipping container; words slide up from below */
  .hero-line {
    display: block;
    overflow: hidden;
    padding-bottom: 0.06em; /* prevent descender clip */
  }
  .hero-line--accent { color: var(--page-accent); }

  /* Word clip wrapper — each word gets its own overflow:hidden row */
  .hero-word-clip {
    display: inline-block;
    overflow: hidden;
    vertical-align: bottom;
    margin-right: 0.22em;
    padding-bottom: 0.04em;
  }

  .hero-word {
    display: inline-block;
    will-change: transform;
  }

  /* Subhead */
  .hero-subhead {
    margin: 0;
    max-width: 43rem;
    font-size: clamp(1.05rem, 0.96rem + 0.4vw, 1.22rem);
    line-height: 1.82;
    color: rgba(246, 247, 250, 0.78);
  }

  /* CTAs */
  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .hero-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 3.4rem;
    padding: 0.95rem 1.55rem;
    border: var(--page-thick-border);
    font-family: var(--font-tech);
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    text-decoration: none;
    transition: background-color 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease, color 0.25s ease;
  }

  .hero-button--primary {
    background: var(--page-accent);
    color: #0F1112;
    border-color: #1A1A1A;
    box-shadow: 0 14px 36px rgba(64, 196, 196, 0.22);
  }
  .hero-button--primary:hover {
    background: #53D3D3;
    box-shadow: 0 8px 28px rgba(64, 196, 196, 0.42);
  }

  .hero-button--ghost {
    border-color: rgba(246,247,250,0.55);
    color: #F6F7FA;
    background: transparent;
  }
  .hero-button--ghost:hover {
    border-color: var(--page-accent);
    background: rgba(64, 196, 196, 0.1);
    color: #FFFFFF;
  }

  /* Video side */
  .hero-media {
    position: relative;
    display: grid;
    gap: 1rem;
  }

  .hero-video-frame {
    position: relative;
    overflow: hidden;
    min-height: clamp(24rem, 52vw, 44rem);
    border: var(--page-thick-border);
    border-radius: 1.6rem;
    box-shadow: 0 30px 72px rgba(0, 0, 0, 0.45);
  }

  .hero-video-frame::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(17,18,20,0.04), rgba(17,18,20,0.2));
    pointer-events: none;
  }

  /* Glow overlay for the pulse animation */
  .hero-video-glow {
    position: absolute;
    inset: 0;
    border-radius: inherit;
    pointer-events: none;
  }

  .hero-video {
    width: 100%;
    height: 100%;
    min-height: inherit;
    object-fit: cover;
    display: block;
  }

  /* Stat pills below video */
  .hero-stat-rail {
    display: flex;
    gap: 0.6rem;
    flex-wrap: wrap;
  }

  .hero-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    flex: 1;
    padding: 0.7rem 0.6rem;
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(64,196,196,0.15);
    border-radius: 0.5rem;
    gap: 0.15rem;
    transition: background 0.2s ease;
  }
  .hero-stat:hover { background: rgba(64,196,196,0.07); }

  .hero-stat__val {
    font-family: var(--font-display, 'Archivo', sans-serif);
    font-size: 1.25rem;
    font-weight: 800;
    letter-spacing: -0.03em;
    color: #40C4C4;
    line-height: 1;
  }

  .hero-stat__label {
    font-family: 'Courier New', monospace;
    font-size: 0.58rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(246,247,250,0.4);
    text-align: center;
  }

  /* Scroll cue */
  .hero-scroll-cue {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.4rem;
    z-index: 2;
  }

  .hero-scroll-label {
    font-family: 'Courier New', monospace;
    font-size: 0.55rem;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: rgba(246,247,250,0.25);
  }

  .hero-scroll-line {
    display: block;
    width: 1px;
    height: 36px;
    background: linear-gradient(180deg, rgba(64,196,196,0.6), transparent);
    transform-origin: top center;
  }

  /* ═══════════════════════════════════════════════════
     WHAT I DO
     ═══════════════════════════════════════════════════ */
  .work-shell {
    display: grid;
    gap: clamp(3rem, 6vw, 5rem);
  }

  .work-header {
    display: grid;
    gap: 1.4rem;
    max-width: 52rem;
  }

  /* Typewriter animation for section title lines */
  .typewriter-line {
    display: inline-block;
    overflow: hidden;
    white-space: nowrap;
    width: 0;
    border-right: 3px solid var(--page-accent);
    transition: none;
  }

  .typewriter-line.typing {
    animation: typewriter-expand 0.6s steps(14, end) forwards;
  }

  .typewriter-line.typed {
    width: auto;
    border-right-color: transparent;
  }

  .typewriter-line.typing:last-of-type {
    animation: typewriter-expand 0.6s steps(14, end) forwards, blink-cursor 0.6s step-end 3;
  }

  @keyframes typewriter-expand {
    from { width: 0; }
    to { width: 100%; }
  }

  @keyframes blink-cursor {
    50% { border-right-color: transparent; }
  }

  /* Scrollable service card gallery */
  .services-scroll {
    display: flex;
    gap: clamp(1rem, 2vw, 1.5rem);
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-padding-inline-start: clamp(0.75rem, 2vw, 2rem);
    padding-bottom: 1rem;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: thin;
    scrollbar-color: var(--page-accent) rgba(0, 0, 0, 0.08);
  }

  .services-scroll::-webkit-scrollbar { height: 4px; }
  .services-scroll::-webkit-scrollbar-track { background: rgba(0, 0, 0, 0.06); border-radius: 2px; }
  .services-scroll::-webkit-scrollbar-thumb { background: var(--page-accent); border-radius: 2px; }

  .service-card {
    flex: 0 0 min(85vw, 420px);
    scroll-snap-align: start;
    display: grid;
    gap: 1rem;
    align-content: start;
    padding: clamp(1.5rem, 3vw, 2rem);
    border: var(--page-thick-border);
    border-left: 5px solid var(--page-accent);
    background: var(--page-surface);
    cursor: pointer;
    transition: box-shadow 0.3s ease, transform 0.3s ease;
  }

  .service-card:hover {
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.1);
    transform: translateY(-3px);
  }

  .service-card__eyebrow {
    margin: 0;
    font-family: var(--font-tech);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--page-accent);
  }

  .service-card__headline {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.3rem, 1rem + 1vw, 1.8rem);
    line-height: 1.08;
    letter-spacing: -0.02em;
    color: var(--page-text);
  }

  .service-card__desc {
    margin: 0;
    font-size: 0.92rem;
    line-height: 1.65;
    color: var(--page-text-muted);
  }

  .service-card__cta {
    display: inline-flex;
    align-items: center;
    margin-top: 0.5rem;
    font-family: var(--font-tech);
    font-size: 0.76rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--page-accent);
    transition: color 0.2s ease;
  }

  .service-card:hover .service-card__cta {
    color: var(--page-text);
  }

  /* ═══════════════════════════════════════════════════
     THE WORKSHOP
     ═══════════════════════════════════════════════════ */
  .eyebrow--light { color: rgba(246, 247, 250, 0.72); }
  .eyebrow--light::before { background: var(--page-accent); }

  .workshop-shell {
    display: grid;
    gap: clamp(3rem, 6vw, 5rem);
  }

  .workshop-header {
    display: grid;
    gap: 1.4rem;
    max-width: 52rem;
  }

  .workshop-grid {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    grid-template-rows: auto auto;
    gap: 1rem;
  }

  .workshop-card {
    margin: 0;
    overflow: hidden;
    border: 2px solid rgba(64, 196, 196, 0.15);
    border-radius: 1rem;
    position: relative;
  }

  .workshop-card--hero {
    grid-row: 1 / 3;
  }

  .workshop-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    aspect-ratio: 4 / 3;
    transition: transform 0.4s ease;
  }

  .workshop-card--hero img {
    aspect-ratio: auto;
    min-height: 100%;
  }

  .workshop-card:hover img {
    transform: scale(1.03);
  }

  .workshop-card figcaption {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 1rem 1.2rem;
    background: linear-gradient(0deg, rgba(17, 18, 20, 0.88), transparent);
    display: grid;
    gap: 0.2rem;
  }

  .workshop-card__label {
    font-family: var(--font-tech);
    font-size: 0.64rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--page-accent);
  }

  .workshop-card__note {
    font-size: 0.82rem;
    line-height: 1.4;
    color: rgba(246, 247, 250, 0.7);
  }

  .workshop-stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 1.2rem;
    padding-top: 1rem;
    border-top: 1px solid rgba(64, 196, 196, 0.2);
  }

  .workshop-stat {
    display: grid;
    gap: 0.3rem;
  }

  .workshop-stat__value {
    font-family: var(--font-display);
    font-size: clamp(2rem, 1.4rem + 2vw, 3rem);
    line-height: 1;
    letter-spacing: -0.03em;
    color: var(--page-accent);
  }

  .workshop-stat__label {
    font-family: var(--font-tech);
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(246, 247, 250, 0.5);
  }

  @media (max-width: 640px) {
    .workshop-grid {
      grid-template-columns: 1fr;
    }
    .workshop-card--hero {
      grid-row: auto;
    }
  }

  /* ═══════════════════════════════════════════════════
     CONTACT (FINAL CTA)
     ═══════════════════════════════════════════════════ */
  .final-cta-shell { text-align: center; }

  .final-cta-copy {
    display: grid;
    gap: 1.5rem;
    justify-items: center;
    max-width: 52rem;
    margin: 0 auto;
  }

  .final-cta-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
  }

  .final-cta-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 3.4rem;
    padding: 0.95rem 1.55rem;
    border: var(--page-thick-border);
    font-family: var(--font-tech);
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    transition: all 0.3s ease;
  }

  .final-cta-button:hover { transform: translateY(-2px) scale(1.02); }

  .final-cta-button--light {
    background: #FFFFFF;
    color: #1A1A1A;
    border-color: #1A1A1A;
  }

  .final-cta-button--light:hover {
    background: #F6F7FA;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
  }

  .final-cta-button--ghost {
    border-color: #FFFFFF;
    color: #FFFFFF;
    background: transparent;
  }

  .final-cta-button--ghost:hover { background: rgba(255, 255, 255, 0.12); }

  /* ═══════════════════════════════════════════════════
     DRAWER
     ═══════════════════════════════════════════════════ */
  .drawer {
    position: fixed;
    inset: 0;
    z-index: 100;
    display: flex;
    justify-content: flex-end;
    pointer-events: none;
  }

  .drawer[data-open='true'] { pointer-events: all; }

  .drawer__backdrop {
    position: absolute;
    inset: 0;
    background: rgba(17, 18, 20, 0.6);
    backdrop-filter: blur(4px);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .drawer[data-open='true'] .drawer__backdrop { opacity: 1; }

  .drawer__panel {
    position: relative;
    width: min(420px, 92vw);
    height: 100%;
    background: var(--page-bg-dark);
    border-left: 3px solid var(--page-accent);
    overflow-y: auto;
    padding: 2rem 1.5rem;
    transform: translateX(100%);
    transition: transform 0.32s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .drawer[data-open='true'] .drawer__panel { transform: translateX(0); }

  .drawer__close {
    position: absolute;
    top: 1rem;
    right: 1rem;
    width: 2.5rem;
    height: 2.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2px solid var(--page-accent);
    background: transparent;
    color: #F6F7FA;
    font-size: 1.1rem;
    cursor: pointer;
    border-radius: 0.5rem;
    transition: all 0.2s ease;
  }

  .drawer__close:hover { background: var(--page-accent); color: #0F1112; }

  :global(.drawer-copy) {
    display: grid;
    gap: 1.2rem;
    color: #F6F7FA;
    padding-top: 1rem;
  }

  :global(.drawer-copy__eyebrow) {
    font-family: var(--font-tech);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--page-accent);
  }

  :global(.drawer-copy__title) {
    margin: 0;
    font-family: var(--font-display);
    font-size: 1.5rem;
    line-height: 1.05;
    color: #F6F7FA;
  }

  :global(.drawer-copy__stakes) {
    margin: 0;
    font-size: 0.94rem;
    line-height: 1.65;
    color: rgba(246, 247, 250, 0.78);
  }

  :global(.drawer-copy__gallery) { display: grid; gap: 0.6rem; }

  :global(.drawer-copy__gallery figure) {
    margin: 0;
    overflow: hidden;
    border: 2px solid #1A1A1A;
    border-radius: 0.8rem;
  }

  :global(.drawer-copy__gallery img) {
    width: 100%;
    display: block;
    aspect-ratio: 16 / 10;
    object-fit: cover;
  }

  :global(.drawer-copy__notes) {
    display: grid;
    gap: 1rem;
    padding: 1rem 0;
    border-top: 1px solid rgba(64, 196, 196, 0.2);
  }

  :global(.drawer-copy__notes span) {
    display: block;
    font-family: var(--font-tech);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--page-accent);
    margin-bottom: 0.3rem;
  }

  :global(.drawer-copy__notes p) {
    margin: 0;
    font-size: 0.94rem;
    line-height: 1.6;
    color: rgba(246, 247, 250, 0.78);
  }

  :global(.drawer-copy__cta) {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.8rem 1.2rem;
    background: var(--page-accent);
    color: #0F1112;
    border: 2px solid #1A1A1A;
    font-family: var(--font-tech);
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    transition: all 0.25s ease;
    width: 100%;
    text-align: center;
  }

  :global(.drawer-copy__cta:hover) {
    background: #53D3D3;
    transform: translateY(-2px);
  }

  /* ═══════════════════════════════════════════════════
     LIGHTBOX
     ═══════════════════════════════════════════════════ */
  .lightbox {
    position: fixed;
    inset: 0;
    z-index: 200;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
  }

  .lightbox[data-open='true'] { pointer-events: all; }

  .lightbox__backdrop {
    position: absolute;
    inset: 0;
    background: rgba(15, 17, 18, 0.92);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    opacity: 0;
    transition: opacity 0.28s ease;
    cursor: pointer;
  }

  .lightbox[data-open='true'] .lightbox__backdrop { opacity: 1; }

  .lightbox__container {
    position: relative;
    z-index: 1;
    max-width: min(90vw, 900px);
    max-height: 85vh;
    display: flex;
    flex-direction: column;
    gap: 0;
    transform: scale(0.92) translateY(20px);
    opacity: 0;
    transition: transform 0.32s cubic-bezier(0.16, 1, 0.3, 1), opacity 0.28s ease;
  }

  .lightbox[data-open='true'] .lightbox__container {
    transform: scale(1) translateY(0);
    opacity: 1;
  }

  .lightbox__close {
    position: absolute;
    top: -2.5rem;
    right: 0;
    background: none;
    border: none;
    color: #F6F7FA;
    font-size: 1.6rem;
    cursor: pointer;
    padding: 0.4rem;
    line-height: 1;
    opacity: 0.7;
    transition: opacity 0.2s;
  }

  .lightbox__close:hover { opacity: 1; }

  .lightbox__img {
    width: 100%;
    max-height: 65vh;
    object-fit: contain;
    border-radius: 1rem 1rem 0 0;
    background: #111214;
  }

  .lightbox__info {
    background: #1A1C1E;
    padding: 1.2rem 1.6rem;
    border-radius: 0 0 1rem 1rem;
    border-top: 2px solid rgba(64, 196, 196, 0.3);
  }

  .lightbox__label {
    display: block;
    font-family: var(--font-tech);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--page-accent);
    margin-bottom: 0.4rem;
  }

  .lightbox__note {
    margin: 0;
    font-size: 0.9rem;
    line-height: 1.55;
    color: rgba(246, 247, 250, 0.75);
  }

  .workshop-card[data-lightbox] { cursor: pointer; }

  /* ═══════════════════════════════════════════════════
     KEYFRAMES
     ═══════════════════════════════════════════════════ */
  @keyframes scroll-in-left {
    from { opacity: 0; transform: translateX(-24px); }
    to   { opacity: 1; transform: none; }
  }

  /* ═══════════════════════════════════════════════════
     RESPONSIVE
     ═══════════════════════════════════════════════════ */
  @media (max-width: 1023px) {
    .hero-shell { grid-template-columns: 1fr; }
    .hero-media { max-width: 32rem; }
    .services-grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 640px) {
    .hero-stat-rail { gap: 0.4rem; }
    .hero-stat__val { font-size: 1.05rem; }
    .services-grid { gap: 1rem; }
    .gallery-item { flex: 0 0 80vw; }
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal-up, .reveal-scale, .reveal-left, .reveal-fade, .reveal-mask {
      opacity: 1 !important;
      transform: none !important;
      clip-path: none !important;
      transition: none !important;
    }
    .hero-bg-img { animation: none !important; }
  }
</style>

<style is:global>
  /* ─── CampaignStrip React component ─── */

  .cstrip-root {
    background: #0A0B0C;
    border-top: 3px solid #40C4C4;
    overflow: hidden;
    position: relative;
  }

  .cstrip-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.55rem 1.6rem;
    border-bottom: 1px solid rgba(255,255,255,0.06);
    position: relative;
    overflow: hidden;
  }

  .cstrip-label {
    font-family: 'Courier New', monospace;
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: rgba(246,247,250,0.3);
  }

  .cstrip-status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: 'Courier New', monospace;
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #40C4C4;
  }

  .cstrip-pulse {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #40C4C4;
    flex-shrink: 0;
    display: inline-block;
  }

  .cstrip-rail {
    display: flex;
    overflow-x: auto;
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
    align-items: stretch;
  }
  .cstrip-rail::-webkit-scrollbar { display: none; }

  .cstrip-cell-wrap {
    display: flex;
    border-right: 1px solid rgba(255,255,255,0.05);
    position: relative;
  }
  .cstrip-cell-wrap--open { border-right: 1px dashed rgba(64,196,196,0.08); }
  .cstrip-cell-wrap:last-child { border-right: none; }

  .cstrip-motion-cell {
    width: 100%;
    position: relative;
    cursor: pointer;
  }
  .cstrip-motion-cell.done:hover    { box-shadow: inset 0 -2px 0 #40C4C4; }
  .cstrip-motion-cell.prusa:hover   { box-shadow: inset 0 -2px 0 #FA6831; }
  .cstrip-motion-cell.open          { cursor: default; }

  .cstrip-cell-link {
    display: block;
    width: 100%;
    height: 100%;
    text-decoration: none;
    color: inherit;
    background: none;
    border: none;
    font-family: inherit;
    text-align: left;
    cursor: pointer;
    position: relative;
    overflow: hidden;
  }

  .cstrip-accent { width: 100%; }

  .cstrip-inner {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 0.75rem;
    align-items: center;
    padding: 1.1rem 1.4rem 1.3rem;
  }

  .cstrip-left {
    display: flex;
    flex-direction: column;
    gap: 0.28rem;
    min-width: 0;
  }

  .cstrip-right {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.5rem;
    flex-shrink: 0;
  }

  .cstrip-badge {
    display: inline-flex;
    align-items: center;
    font-family: 'Courier New', monospace;
    font-size: 0.58rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    padding: 0.18rem 0.45rem;
    width: fit-content;
    line-height: 1.3;
    border-radius: 2px;
  }
  .cstrip-badge--done     { }
  .cstrip-badge--upcoming { border: 1px solid; }
  .cstrip-badge--open     { color: rgba(246,247,250,0.28); border: 1px dashed rgba(246,247,250,0.15); }

  .cstrip-date {
    font-family: 'Courier New', monospace;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: rgba(246,247,250,0.3);
  }

  .cstrip-title {
    font-family: var(--font-display, 'Archivo', sans-serif);
    font-size: 1rem;
    font-weight: 800;
    letter-spacing: -0.01em;
    line-height: 1.15;
    color: #F6F7FA;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .cstrip-title--ghost {
    color: rgba(246,247,250,0.2);
    font-weight: 700;
    letter-spacing: 0.05em;
    font-size: 0.85rem;
    font-family: 'Courier New', monospace;
    transition: color 0.2s;
  }
  .cstrip-motion-cell.open:hover .cstrip-title--ghost { color: rgba(246,247,250,0.5); }

  .cstrip-sub {
    font-family: var(--font-tech, 'Archivo', sans-serif);
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    color: rgba(246,247,250,0.3);
    line-height: 1.35;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .cstrip-sub--ghost {
    color: rgba(246,247,250,0.12);
    font-family: 'Courier New', monospace;
    font-size: 0.62rem;
    letter-spacing: 0.1em;
  }

  .cstrip-logo {
    height: 24px;
    width: auto;
    max-width: 80px;
    opacity: 0.5;
    filter: brightness(0) invert(1);
    object-fit: contain;
    transition: opacity 0.2s;
  }
  .cstrip-motion-cell:hover .cstrip-logo { opacity: 0.95; }
  .cstrip-logo--color { filter: none; opacity: 0.65; }

  .cstrip-img {
    width: 84px;
    height: 58px;
    object-fit: cover;
    border: 2px solid rgba(64,196,196,0.2);
    display: block;
    flex-shrink: 0;
  }

  .cstrip-expand {
    font-size: 0.72rem;
    font-family: 'Courier New', monospace;
    display: block;
    transition: color 0.2s;
  }
  .cstrip-expand--ghost { color: rgba(246,247,250,0.15); transition: color 0.2s; }

  .cstrip-dashes {
    font-family: 'Courier New', monospace;
    font-size: 1rem;
    color: rgba(246,247,250,0.08);
    letter-spacing: 0.2em;
    transition: color 0.2s;
  }
  .cstrip-motion-cell.open:hover .cstrip-dashes { color: rgba(64,196,196,0.3); }

  .cstrip-scan {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: linear-gradient(105deg, transparent 35%, rgba(64,196,196,0.07) 50%, transparent 65%);
  }

  .cstrip-track {
    height: 3px;
    background: rgba(255,255,255,0.04);
    overflow: hidden;
  }
  .cstrip-track-fill {
    height: 100%;
    background: linear-gradient(90deg, #1a7a7a, #40C4C4, rgba(64,196,196,0.35));
    position: relative;
    overflow: hidden;
  }
  .cstrip-shimmer {
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, transparent 0%, rgba(255,255,255,0.35) 50%, transparent 100%);
  }

  /* ─── Modal overlay ─── */
  .tm-overlay {
    position: fixed;
    inset: 0;
    z-index: 500;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem;
    background: rgba(8,10,11,0.88);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    cursor: pointer;
  }

  .tm-panel {
    position: relative;
    background: #FFFFFF;
    color: #111214;
    border: 3px solid #111214;
    max-width: 560px;
    width: 100%;
    max-height: 90vh;
    overflow-y: auto;
    cursor: default;
    scrollbar-width: thin;
    scrollbar-color: #111214 transparent;
  }
  .tm-panel--wide { max-width: 700px; }

  .tm-close {
    position: absolute;
    top: 0; right: 0;
    width: 2.8rem;
    height: 2.8rem;
    background: #111214;
    border: none;
    color: #F6F7FA;
    font-size: 1.4rem;
    line-height: 1;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 2;
  }

  .tm-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem 1.4rem;
    padding-right: 3.8rem;
    background: #111214;
    border-bottom: 3px solid;
  }

  .tm-head-date {
    font-family: 'Courier New', monospace;
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: rgba(246,247,250,0.5);
  }

  .tm-head-tag {
    font-family: 'Courier New', monospace;
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    border: 1px solid;
    padding: 0.2rem 0.5rem;
  }

  .tm-head-logo {
    height: 26px;
    width: auto;
    max-width: 100px;
    filter: brightness(0) invert(1);
    opacity: 0.85;
    object-fit: contain;
  }
  .tm-head-logo--color { filter: none; }

  .tm-photo { height: 210px; overflow: hidden; border-bottom: 3px solid #111214; }
  .tm-photo img { width: 100%; height: 100%; object-fit: cover; display: block; }

  .tm-body { padding: 1.6rem 1.8rem 2rem; }

  .tm-title {
    font-family: var(--font-display, 'Archivo', sans-serif);
    font-size: 1.85rem;
    font-weight: 800;
    letter-spacing: -0.035em;
    text-transform: uppercase;
    color: #111214;
    margin: 0 0 0.3rem;
    line-height: 1.0;
  }

  .tm-location {
    font-family: 'Courier New', monospace;
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(17,18,20,0.38);
    margin: 0 0 1.2rem;
  }

  .tm-text {
    font-size: 0.95rem;
    line-height: 1.78;
    color: rgba(17,18,20,0.72);
    margin: 0 0 1rem;
    padding-top: 1.1rem;
    border-top: 1px solid rgba(17,18,20,0.07);
  }
  .tm-text:first-of-type { border-top: none; padding-top: 0; }

  .tm-facts {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    border: 2px solid #111214;
    margin: 1.4rem 0;
  }
  .tm-fact {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 1rem 0.5rem 0.85rem;
    gap: 0.25rem;
    border-right: 2px solid #111214;
  }
  .tm-fact:last-child { border-right: none; }
  .tm-fact-val {
    font-family: var(--font-display, 'Archivo', sans-serif);
    font-size: 1.65rem;
    font-weight: 800;
    letter-spacing: -0.04em;
    color: #111214;
    line-height: 1;
    display: block;
  }
  .tm-fact-key {
    font-family: 'Courier New', monospace;
    font-size: 0.58rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgba(17,18,20,0.38);
    text-align: center;
  }

  .tm-links { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: 1.5rem; }
  .tm-cta {
    display: inline-flex;
    align-items: center;
    padding: 0.6rem 1rem;
    background: transparent;
    color: #111214;
    border: 2px solid rgba(17,18,20,0.18);
    font-family: var(--font-tech, 'Archivo', sans-serif);
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    text-decoration: none;
    transition: border-color 0.2s, background 0.2s;
  }
  .tm-cta:hover { background: rgba(17,18,20,0.05); border-color: #111214; }
  .tm-cta--primary { background: #111214; color: #F6F7FA; border-color: #111214; }
  .tm-cta--primary:hover { background: #2a2b2d; }
  .tm-cta--orange { background: #FA6831; color: #FFF; border-color: #FA6831; }
  .tm-cta--orange:hover { background: #e85e2a; border-color: #e85e2a; }

  @media (max-width: 700px) {
    .cstrip-inner { padding: 0.9rem 1rem 1rem; gap: 0.5rem; }
    .cstrip-img   { width: 60px; height: 42px; }
  }
  @media (max-width: 520px) {
    .tm-facts { grid-template-columns: repeat(2, 1fr); }
    .tm-fact:nth-child(2) { border-right: none; }
    .tm-fact:nth-child(3),
    .tm-fact:nth-child(4) { border-top: 2px solid #111214; }
    .tm-fact:nth-child(4) { border-right: none; }
    .tm-title { font-size: 1.5rem; }
  }
  @media (prefers-reduced-motion: reduce) {
    .cstrip-pulse { animation: none; box-shadow: 0 0 0 2px rgba(64,196,196,0.5); }
    .cstrip-scan  { display: none; }
  }
</style>
